[project]
name = "nuclear-autoresearch"
version = "0.1.0"
description = "Autonomous neural network research for nuclear physics emulation"
readme = "../README.md"
requires-python = ">=3.10,<3.14"
dependencies = [
    "mlx>=0.30.0",
    "numpy>=2.2.6",
]
